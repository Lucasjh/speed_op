<%= render 'highcharts_index' %>

<div id="URLs" align="center"></div>

<hr>

<div class="page-header">
  <% if can? :create, Site %>
  <%= link_to new_site_path, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Site
      <% end %>
  <% end %>
  <h1>Your Sites</h1>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
    <tr>
      <th>Name</th>
      <th>URL</th>
      <th>Mobile Speed</th>
      <th>Show</th>
      <% if can? :update, :destroy, Site %>
    <th>Edit</th>
      <th>Delete</th>
        <% end %>
    </tr>
    </thead>

    <tbody>
    <%= content_tag_for(:tr, @sites) do |site| %>
      <td><%= site.name %></td>
      <td><%= site.url %></td>
      <td><%= site.current_mobile_score %></td>
      <td><%= link_to '', site, class: 'btn btn-primary glyphicon glyphicon-search' %></td>
      <% if can? :update, :destroy, Site %>
      <td><%= link_to '', edit_site_path(site), class: 'btn btn-primary glyphicon glyphicon-edit' %></td>
      <td><%= link_to '', site, method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-primary glyphicon glyphicon-trash' %></td>
    <% end %>
      <% end %>

    </tbody>
  </table>

      <%= link_to " Export Stats", sites_path(format: "csv"), class: 'btn btn-primary glyphicon glyphicon-export' %>
</div>
